# 这是一个教师类
class Teacher:
    # 初始化属性
    def __init__(self,teacher_id,teacher_password,teacher_name,teacher_age,teacher_sex,teacher_address,teacher_major):
        self.teacher_id = teacher_id
        self.teacher_password = teacher_password
        self.teacher_name = teacher_name
        self.teacher_age = teacher_age
        self.teacher_sex = teacher_sex
        self.teacher_adress = teacher_address
        self.teacher_major = teacher_major
